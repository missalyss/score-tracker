import React from 'react';
import Header from './header';
import PlayerWrapper from './player-wrapper';

const Root = () => {
  return (
    <div className="Root">
      <Header />
      <PlayerWrapper />
    </div>
  );
}

export default Root;
